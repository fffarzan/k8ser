apiVersion: v1
kind: Service
metadata:
  name: mongo-express-ext-service
spec:
  selector:
    app: mongodb
  type: LoadBalancer
  ports:
    - protocol: TCP
      port: 8081
      targetPort: 8081
      nodePort: 30000 # for external IP address
